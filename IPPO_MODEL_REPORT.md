# 첨부 이미지 기준 IPPO 3D 모델 — 2026-09-17

첨부 IPPO 이미지를 기준으로 실제 게임 리그를 재구성하고 [미리보기](ippo.html)와 [GLB 모델](assets/models/ippo-reference-v1.glb)을 만들었습니다. 원본 이미지 사본은 공개 배포에서 제외한 로컬 `artifacts/character-rework/ippo-reference.png`에 보관합니다.

## 모델과 표현

- 목·흉곽·가슴·복부·삼각근·상완·팔꿈치·전완은 별도 덩어리의 외곽선을 없애고 하나의 용접된 연속 표면으로 구성했습니다. 암시적 곡면을 직접 샘플링해 지오메트리를 만들고 8개 뼈에 최대 4개 가중치를 정규화해 배분합니다. 가슴·복부는 실제 볼륨과 완만한 곡면이며 얇은 외곽선은 실루엣에만 나타납니다.
- 상체는 18,936 정점·37,868 삼각형입니다. 몸통과 팔 사이의 가중치는 표면의 각 부위까지 거리로 정해 펀치 때 흉곽이 팔을 과하게 따라가지 않도록 수정했습니다. 다리는 기존의 연속 스킨 메시를 유지합니다. 눈·눈썹·코·턱의 비율, 눈두덩·코의 돌출과 턱의 테이퍼를 새로 조형했습니다.
- 검은 헤어는 두피에서 자라는 방향성 있는 50개의 뾰족한 덩어리로 만들고 한 지오메트리로 묶습니다. 정수리·관자놀이·앞머리·뒷목의 층을 구분합니다. 얼굴은 원형 홍채·동공·윗눈꺼풀·각진 굵은 눈썹·닫힌 입으로 집중한 인상을 만듭니다. 얼굴 패치 영역을 넓혀 눈과 입이 얼굴 가운데에 몰리던 문제를 줄였습니다. 집중·깜박임·피격 표정은 캐시된 텍스처로 전환합니다.
- 첨부 이미지의 따뜻한 황갈색 피부와 부드러운 셀 명암을 기준으로 IPPO를 기준으로 한 셰이딩을 적용했습니다. 가슴 아래·복부·쇄골·옆구리의 표면 색은 검은 경계선 대신 따뜻한 옅은 음영을 사용합니다. 피부는 무광이며 글러브와 헤어의 하이라이트를 구분합니다.
- IPPO 복장은 이번 첨부 이미지에 맞춰 흰 반바지·빨간 허리밴드·검은 글자가 있는 흰 IPPO 라벨로 변경했습니다. 빨간 글러브와 기존 신발·전투 스타일·기술·능력치는 유지합니다. 다른 캐릭터의 외형 정의는 유지하고 같은 연속 상체·얼굴·헤어 표현 방식으로 확장했습니다.

게임은 [IppoModel.js](js/IppoModel.js)의 실제 지오메트리와 [Rig.js](js/Rig.js)의 원래 포즈 체계를 사용합니다. 사진 평면을 캐릭터 대신 세우는 방식이 아닙니다. 새 메시·헤어 지오메트리는 캐시해 여러 IPPO를 생성할 때 공유합니다.

## 파일과 검증

[GLB](assets/models/ippo-reference-v1.glb)는 2,321,548바이트이며 메시·뼈·피부 가중치·얼굴 텍스처·재질과 `Guard`, `StraightPunch`, `HookPunch` 애니메이션을 포함합니다. 캐릭터 선택이 가능한 [미리보기 페이지](ippo.html)는 게임과 같은 절차적 모델·셀 셰이더를 사용하고 회전·확대·다섯 자세·동작 재생·모바일 표시·GLB 다운로드를 제공합니다. GLB 재로딩에서 피부 메시 3개와 3개 애니메이션, 유효한 좌표를 확인했고 정면과 훅을 다시 렌더링했습니다. 파일 크기와 SHA-256은 [manifest](assets/models/manifest.json)에 있습니다.

`node scripts/character_qa.cjs after`는 정면·측면·가드·스트레이트·훅과 얼굴, 실제 게임 화면을 캡처했습니다. 모든 10개 캐릭터+코치 4개의 행렬·가중치·표정, 흰 과거 잔상·관절 제한·외곽선 페이드·스텐실, 기본/실험 렌더러 및 모바일도 통과했습니다. 로컬 1440×1000, 180프레임에서 60.34FPS·p95 16.70ms, 최대 409 draw calls·553,642 삼각형이었습니다. 실물 모바일 성능 보장은 아닙니다. JS/셰이더 오류는 없었으며 기존 선택적 음성 manifest 404는 남아 있습니다.

`ippo-export` 검증은 GLB 생성·재로딩·동작을, `ippo-viewer` 검증은 자세 버튼·재생·모바일·다운로드를 확인합니다. 수정 전후 이미지는 로컬 `ippo-before-*.png`와 `after-*.png`, 미리보기는 `ippo-viewer-*.png`, GLB는 `ippo-glb-*.png`에 있습니다.

## 한계

사진 한 장을 기준으로 재구성했으므로 뒷면·가려진 부위와 정확한 원본 토폴로지는 추정입니다. 절차적 모델의 근육 조형·손가락·천 주름·헤어의 미세한 가닥은 레퍼런스보다 단순합니다. 깊은 가드·극단적인 펀치에는 겨드랑이의 눌림이나 잔주름이 남을 수 있습니다. 표정은 텍스처 전환이며 얼굴 블렌드셰이프는 없습니다. 게임의 전용 셀 셰이더와 외곽선은 표준 glTF에 포함되지 않아, GLB는 재사용 가능한 PBR 재질로 근사했고 정확한 게임 재질 표현은 `ippo.html`에서 확인할 수 있습니다. 전체 캐릭터 확장과 추가 검증은 [CHARACTER_VISUAL_REPORT.md](CHARACTER_VISUAL_REPORT.md)의 마지막 항목에 기록했습니다.
