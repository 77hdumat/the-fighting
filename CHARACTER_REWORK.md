# 캐릭터 렌더링 재구성 — 레퍼런스 기준

작업일: 2026-09-16
레퍼런스: `ippo.jpeg`, `mashiba.webp`, `miyata.webp`, `sendo.jpeg`
목표 화풍: **하이브리드** — 몸/장비는 리얼한 셰이딩, 얼굴·머리는 셀셰이딩 (사용자 선택)

---

## 1. "지점토처럼 보이던" 진짜 원인

두 가지가 겹쳐 있었다.

### 원인 A — `PHOTOREAL` 경로가 툰 렌더링을 통째로 버린다

`js/Rig.js` 구버전:

```js
} else if (PHOTOREAL) {
  m = new THREE.MeshPhysicalMaterial({ ... });   // 툰 램프 없음
}
...
if (outline && !PHOTOREAL) mesh.add(...)          // 잉크 외곽선 없음
```

`?photoreal=1` 에서는 셀 램프도, 검은 외곽선도 사라진다. 부드러운 PBR 확산광만 남아 정확히 점토 질감이 된다.

### 원인 B — 피부를 단색 1톤으로만 칠하고 밝기만 깎았다 (기본 경로도 해당)

레퍼런스 4장에서 픽셀을 HSL 분류해 추출한 실제 채색:

| | 밝은면 | 중간톤 | 그림자 |
|---|---|---|---|
| IPPO | `#f0caad` | `#d88f6b` | `#a25334` |
| MASHIBA | `#f1c8a6` | `#d8916b` | `#a1502f` |
| MIYATA | `#edd5c1` | `#dc926e` | `#a15030` |
| SENDO | `#f1d0b3` | `#d6906c` | `#a15430` |

네 캐릭터가 거의 같은 램프를 쓴다. 그리고 결정적으로 —

**그림자는 "어두운 살색"이 아니다. 채도가 올라가고 색상이 붉은 쪽으로 이동한다.**
(`#f0cdb0` hue 27° sat 65% → `#a15230` hue 20° sat 54%)

기존 코드는 `skin` 색 하나에 `gradientMap`(밝기 배율)만 곱했다. 밝기 배율은 채도를 올릴 수도, 색상을 밀 수도 없다.
그래서 그림자가 탁한 회갈색이 되고, 그게 점토의 색이다.

---

## 2. 적용한 변경

### 2-1. 3톤 컬러 램프 재매핑 (`js/Rig.js`)

셰이딩이 끝난 휘도를 베이스 색 대비로 정규화한 뒤, 3톤 램프로 다시 매핑한다.

```glsl
float shade = clamp(dot(gl_FragColor.rgb, LUMA) / baseLum, 0.0, 1.6);
vec3 toned = mix(uShadowColor, uMidColor, smoothstep(0.36, 0.58, shade));
toned = mix(toned, uLitColor, smoothstep(0.72, 0.96, shade));
gl_FragColor.rgb = mix(gl_FragColor.rgb, toned, uGrade);
```

- 피부·얼굴은 레퍼런스에서 추출한 램프를 그대로 사용 (`def.ramp`)
- 그 외 부위는 베이스 색에서 유도 (`deriveRamp`: 그림자로 갈수록 채도 +38%, 색상 −7°)
- 그림자맵·다중 광원 등 three 의 조명 계산은 그대로 살아 있다. 그 결과 위에 색만 다시 입히는 방식

### 2-2. 부위별 질감 (하이브리드)

`ROLE_LOOK` 테이블로 역할마다 다른 값을 준다. 역할은 지오메트리 → 색 순으로 판정한다.

| 역할 | 램프 적용률 | 림라이트 | 스페큘러 | 외곽선 |
|---|---|---|---|---|
| skin (몸) | 0.88 | 0.34 | 좁고 약함 | 1.0× |
| face | 0.92 | 0.28 | 거의 없음 | 1.35× |
| hair | 0.55 | 0.14 | 없음 + 광택 띠 | 0.75× |
| glove | 0.62 | 0.38 | 가죽 광택 | 1.15× |
| cloth | 0.45 | 0.20 | 약함 | 1.0× |

`specEdge`(하이라이트 발생 문턱)를 0.74~0.88 로 높게 잡았다. 이 값이 낮으면 구체 부위에 **커다란 흰 원**이 찍혀 즉시 플라스틱처럼 보인다 (1차 시도에서 실제로 발생, `test-shots/13-ippo-face2.png` 참조).

### 2-3. 얼굴을 텍스처로 재구성

기존: 흰자 구체 + 동공 구체 + 눈썹 박스를 얼굴에 박아 넣음 → 눈알이 튀어나온 인형.

변경: `faceTexture(def)` 가 캔버스에 눈·눈썹·입을 그리고, 머리 구면에 정확히 맞는 앞면 패치에 입힌다.

그린 요소 — 레퍼런스 분석 기준:
- **윗눈꺼풀 선이 가장 굵다.** 눈매를 결정하는 건 이 선 하나다
- 흰자는 위가 직선에 가깝고 아래가 둥근 아몬드
- 홍채는 크고 어둡게, 아래 눈꺼풀에 닿도록 내려 앉힘 + 위쪽에 눈꺼풀 그림자
- **캐치라이트 필수.** 없으면 눈이 죽는다
- `eyes: narrow` (마시바·센도) 는 흰자 없이 찢어진 검은 선으로 처리
- 눈썹은 `brows: thick/thin` 에 따라 26px / 13px

### 2-4. 그 외

- **근육 윤곽선**: 대흉근·복근에도 외곽선 부여 (`part(..., 0.72)` / `0.5`). 원작은 근육을 선으로 구분한다
- **외곽선 굵기 차등**: 얼굴·머리 1.35× / 몸 1.0× / 작은 부위 0.38~0.72×. 선 색은 순흑이 아닌 `#120a08`
- **머리 캡 후퇴**: `rotation.x = -0.26` 으로 앞 헤어라인을 올려 이마·눈썹 노출
- **머리 광택 띠**: 좁고 경계가 단단한 가로 띠. 넓게 퍼뜨리면 플라스틱이 된다
- **머리 구체 세그먼트** 22×18 → 40×30. 3톤 램프는 면 경계를 드러내므로 얼굴은 해상도가 필요하다
- **턱 박스 축소**: 머리 구면을 뚫고 나와 얼굴 텍스처 위로 면이 얼룩지던 문제 해결
- **하이라이트 소프트 클립**: 인트로처럼 강한 스포트라이트에서 얼굴이 흰색으로 날아가던 문제 해결
- **MASHIBA `muscle` 0.35 → 0.75**: 레퍼런스는 마르되 근육 윤곽이 뚜렷하다
- **머리색 교정**: MIYATA `#4a3220`(갈색) → `#0c0909`, SENDO `#6b3a12`(갈색) → `#0f0c0e`. 레퍼런스는 둘 다 흑발
- **글러브색 교정**: 레퍼런스는 전원 적색 계열. 캐릭터 구분은 트렁크 색이 담당하므로 식별성 유지

---

## 3. 검증 결과

### 성능 — 회귀 없음. 일부는 개선

| | 작업 전 | 작업 후 |
|---|---|---|
| 전투 중 평균 | 8.48ms / 118.0fps | **8.40ms / 119.0fps** |
| p99 | 14.3ms | **10.5ms** |
| 최악 프레임 | 38.6ms | 36.7ms |
| 셰이더 프로그램 | 14 | 14 |
| **파이터 1인당 재질 수** | **~300** | **8~9** |

재질이 300개에서 8~9개로 줄었다. `(역할, 색)` 단위 캐시를 넣은 결과이며, p99 개선은 여기서 나온 것으로 보인다.

### 하이라이트 클리핑

스포트라이트 강도를 3.5배(인트로 수준)로 올리고 몸통 피부 픽셀을 직접 읽었다.

| 조명 | 픽셀값 | 클리핑 |
|---|---|---|
| 기본 | `rgb(222,158,117)` | 없음 |
| 강광 3.5× | `rgb(224,161,122)` | 없음 |

### 기능

로드 에러 0, 입력·피격·데미지·KO 정상 동작 확인.

### 스크린샷 (`test-shots/`)

| 파일 | 내용 |
|---|---|
| `02-fight-default.png` | **작업 전** 인게임 |
| `04-photoreal-fight.png` | **작업 전** photoreal 경로 |
| `13-ippo-face2.png` | 1차 시도 — 글러브·머리에 흰 원이 찍힌 실패 사례 |
| `19-final-4up.png` | **작업 후** 4인 비교 |
| `15-ippo-bust.png` | 작업 후 IPPO 상반신 |

---

## 4. 작업 도구

`rigview.html` (신규) — 캐릭터 룩 전용 뷰어.
인게임은 타격 이펙트·스피드라인이 화면을 덮어 캐릭터 판정이 불가능하다.
이 페이지는 인게임 링 조명을 근사한 환경에서 4인을 나란히 세우고, 전신/상반신/얼굴 뷰를 실제 본 좌표에서 계산해 잡는다.
개발 전용이며 배포물(`index.html`)에는 포함되지 않는다.

---

## 5. 2차 작업 — 지오메트리 격차 해소

1차(셰이딩·채색·얼굴) 이후 남아 있던 실루엣 격차를 처리했다.

| 항목 | 이전 | 적용 |
|---|---|---|
| 몸통 실루엣 | 폭이 일정한 통짜 박스 | `taperY()` 로 정점을 높이에 따라 눌러 역삼각형. `muscle` 값이 높을수록 차이 확대 |
| 삼각근 | 팔에 묻혀 안 보임 | 반지름 확대 + 바깥·위로 이동, 가로로 부푼 타원 |
| 목 | 거의 없음 | 굵기 `torsoW` 연동, 아래가 넓은 원뿔대 |
| 글러브 | 구체 | 라운드 박스 + 손목 쪽으로 좁아지는 테이퍼 (맨손 캐릭터는 구체 유지) |
| 대흉근 | 평평한 박스 | 아래로 좁아지게 테이퍼 → 가슴 아래 선 생성 |

### 캐릭터별 헤어 신규 작성

| 캐릭터 | 스타일 | 형태 |
|---|---|---|
| IPPO | `spiky` (강화) | 사방으로 크고 굵게 뻗친 스파이크 11개 + 이마로 흘러내린 앞머리 3가닥 |
| MASHIBA | `longblack` (신규) | 정수리 스파이크 + 얼굴 양옆 긴 머리칼(턱선 아래) + 관자놀이 앞머리 |
| MIYATA | `bowl` (신규) | 이마를 덮는 일자 앞머리 + 귀를 덮는 중단발 + 뒷머리 |
| SENDO | `mane` (신규) | 뒤·위로 젖혀진 갈기 11가닥 (**머리 뒤쪽 아크에만 배치**) + 짧은 앞머리 |

### 작업 중 잡은 결함

- **글러브 외곽선이 격자로 깨짐** — 인버티드 헐 외곽선이 각진 박스의 하드 엣지에서 벌어진 것.
  라운드 반경·세그먼트를 키우고 글러브 외곽선 굵기를 1.15× → 0.62× 로 낮춰 해소
- **MIYATA 머리가 얼굴 전체를 덮는 검은 헬멧** — 캡이 적도 아래까지 내려옴. `thetaLength` 0.66π → 0.46π
- **SENDO 갈기가 얼굴 우측을 덮음** — 스파이크를 원형으로 둘러 앞쪽 가닥이 얼굴에 옴.
  머리 뒤쪽 아크에만 배치하도록 변경
- **삼각근이 머리만 한 공** — 1차 조정이 과했음. 반지름 0.128 → 0.106

### 성능 (2차 작업 후)

| | 1차 후 | 2차 후 |
|---|---|---|
| 전투 중 평균 | 8.40ms / 119.0fps | **8.37ms / 119.5fps** |
| p99 | 10.5ms | **10.3ms** |
| 씬 폴리곤 | 276,652 | 402,448 |

폴리곤이 45% 늘었는데 프레임타임은 그대로다. 이 하드웨어에서 병목은 폴리곤이 아니다.

### 스크린샷

| 파일 | 내용 |
|---|---|
| `19-final-4up.png` | 1차 작업 후 (셰이딩·얼굴만) |
| `21-geo-v1b.png` | 2차 1차 시도 — MIYATA 헬멧 머리, 글러브 외곽선 격자 |
| `24-sendo-face.png` | SENDO 갈기가 얼굴을 덮던 상태 |
| `26-geo-v3.png` | **최종 4인** |
| `25-sendo-bust.png` | 최종 SENDO 상반신 |

---

## 6. 아직 남은 것

- 팔·다리는 여전히 캡슐 하나 — 원작의 전완근/이두 구분 없음
- 가드 포즈에서 어깨가 글러브에 가려 역삼각형이 덜 읽힘 (포즈 문제)
- 히든 캐릭터 6인은 이번 작업 범위 밖 (팔레트·헤어 미조정)
